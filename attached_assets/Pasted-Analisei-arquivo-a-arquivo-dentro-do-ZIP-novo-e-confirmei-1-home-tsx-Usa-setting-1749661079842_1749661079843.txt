Analisei arquivo a arquivo dentro do ZIP novo e confirmei:

1. **`home.tsx`**

   * Usa `settings?.headline1` e `settings?.headline3` nos fallbacks.
   * Não há nenhum hard-coded “Conectando…” sobrando – tudo está dinâmico.

2. **`footer.tsx`**

   * Substitui o texto do rodapé por

     ```tsx
     {settings?.footerDescription?.trim()
       ? settings.footerDescription
       : "Conectando você às melhores empresas da cidade com sustentabilidade e qualidade."}
     ```
   * Exibe `settings.siteName` e `settings.locality` dinamicamente.

3. **`advertise.tsx`**

   * Substitui hero texts por

     ```tsx
     {settings?.advertiseHeadline || "SUA MARCA EM DESTAQUE ENTRE AS MELHORES"}
     {settings?.advertiseSubtitle1 || "Onde excelência encontra visibilidade!"}
     {settings?.advertiseSubtitle2 || "Junte-se à nossa comunidade sustentável..."}
     ```
   * Gera o FAQ a partir de

     ```ts
     const faqs = [
       { question: settings?.faq1Question, answer: settings?.faq1Answer },
       …
     ];
     ```
   * Não restou nenhum texto fixo na seção de perguntas.

4. **`admin.tsx`**

   * O formulário agora inclui campos para `footerDescription`, `advertiseHeadline`, `advertiseSubtitle1` e `advertiseSubtitle2`.
   * Os campos antigos (siteName, locality, headline1/3, taglines, phone, email, address, URLs, FAQs) continuam lá.

5. **`SettingsContext.tsx`**

   * Busca `"/api/admin/settings"` com `queryKey: ["settings"]`.
   * Preenche `settings` completo no contexto, e tema (`logoUrl`, `primaryColor`, etc.) separado.

6. **`schema.ts`**

   * A tabela `site_settings` ganhou colunas `footer_description`, `advertise_headline`, `advertise_subtitle1` e `advertise_subtitle2`.
   * O `insertSiteSettingSchema` omite `id` e `updatedAt` e inclui esses quatro novos campos.

7. **`storage.ts`** (MemStorage)

   * O método `updateSiteSettings` aplica `{ ...existing, ...settingsUpdate, updatedAt: new Date() }`, garantindo que todos os novos campos sejam persistidos.

Não encontrei nenhum texto ainda hard-coded, nem inconsistências de nomes. Tudo está puxando dos campos de admin, com fallbacks corretos e o `updatedAt` sendo atualizado.

👍 Está tudo pronto para enviar ao Replit!
